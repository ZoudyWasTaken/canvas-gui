import { Position, Rect, Util } from "./Util";

export default class TextRendererUtil extends Util {
    constructor(context: CanvasRenderingContext2D) {
        super();
    }
    
    renderString(str: string, size: Rect, pos: Position) {}
}